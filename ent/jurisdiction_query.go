// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/matthewbaird/ontology/ent/jurisdiction"
	"github.com/matthewbaird/ontology/ent/jurisdictionrule"
	"github.com/matthewbaird/ontology/ent/predicate"
	"github.com/matthewbaird/ontology/ent/propertyjurisdiction"
)

// JurisdictionQuery is the builder for querying Jurisdiction entities.
type JurisdictionQuery struct {
	config
	ctx                       *QueryContext
	order                     []jurisdiction.OrderOption
	inters                    []Interceptor
	predicates                []predicate.Jurisdiction
	withChildren              *JurisdictionQuery
	withParentJurisdiction    *JurisdictionQuery
	withRules                 *JurisdictionRuleQuery
	withPropertyJurisdictions *PropertyJurisdictionQuery
	withFKs                   bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the JurisdictionQuery builder.
func (_q *JurisdictionQuery) Where(ps ...predicate.Jurisdiction) *JurisdictionQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *JurisdictionQuery) Limit(limit int) *JurisdictionQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *JurisdictionQuery) Offset(offset int) *JurisdictionQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *JurisdictionQuery) Unique(unique bool) *JurisdictionQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *JurisdictionQuery) Order(o ...jurisdiction.OrderOption) *JurisdictionQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryChildren chains the current query on the "children" edge.
func (_q *JurisdictionQuery) QueryChildren() *JurisdictionQuery {
	query := (&JurisdictionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(jurisdiction.Table, jurisdiction.FieldID, selector),
			sqlgraph.To(jurisdiction.Table, jurisdiction.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, jurisdiction.ChildrenTable, jurisdiction.ChildrenColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryParentJurisdiction chains the current query on the "parent_jurisdiction" edge.
func (_q *JurisdictionQuery) QueryParentJurisdiction() *JurisdictionQuery {
	query := (&JurisdictionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(jurisdiction.Table, jurisdiction.FieldID, selector),
			sqlgraph.To(jurisdiction.Table, jurisdiction.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, jurisdiction.ParentJurisdictionTable, jurisdiction.ParentJurisdictionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRules chains the current query on the "rules" edge.
func (_q *JurisdictionQuery) QueryRules() *JurisdictionRuleQuery {
	query := (&JurisdictionRuleClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(jurisdiction.Table, jurisdiction.FieldID, selector),
			sqlgraph.To(jurisdictionrule.Table, jurisdictionrule.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, jurisdiction.RulesTable, jurisdiction.RulesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPropertyJurisdictions chains the current query on the "property_jurisdictions" edge.
func (_q *JurisdictionQuery) QueryPropertyJurisdictions() *PropertyJurisdictionQuery {
	query := (&PropertyJurisdictionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(jurisdiction.Table, jurisdiction.FieldID, selector),
			sqlgraph.To(propertyjurisdiction.Table, propertyjurisdiction.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, jurisdiction.PropertyJurisdictionsTable, jurisdiction.PropertyJurisdictionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Jurisdiction entity from the query.
// Returns a *NotFoundError when no Jurisdiction was found.
func (_q *JurisdictionQuery) First(ctx context.Context) (*Jurisdiction, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{jurisdiction.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *JurisdictionQuery) FirstX(ctx context.Context) *Jurisdiction {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Jurisdiction ID from the query.
// Returns a *NotFoundError when no Jurisdiction ID was found.
func (_q *JurisdictionQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{jurisdiction.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *JurisdictionQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Jurisdiction entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Jurisdiction entity is found.
// Returns a *NotFoundError when no Jurisdiction entities are found.
func (_q *JurisdictionQuery) Only(ctx context.Context) (*Jurisdiction, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{jurisdiction.Label}
	default:
		return nil, &NotSingularError{jurisdiction.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *JurisdictionQuery) OnlyX(ctx context.Context) *Jurisdiction {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Jurisdiction ID in the query.
// Returns a *NotSingularError when more than one Jurisdiction ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *JurisdictionQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{jurisdiction.Label}
	default:
		err = &NotSingularError{jurisdiction.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *JurisdictionQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Jurisdictions.
func (_q *JurisdictionQuery) All(ctx context.Context) ([]*Jurisdiction, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Jurisdiction, *JurisdictionQuery]()
	return withInterceptors[[]*Jurisdiction](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *JurisdictionQuery) AllX(ctx context.Context) []*Jurisdiction {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Jurisdiction IDs.
func (_q *JurisdictionQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(jurisdiction.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *JurisdictionQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *JurisdictionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*JurisdictionQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *JurisdictionQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *JurisdictionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *JurisdictionQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the JurisdictionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *JurisdictionQuery) Clone() *JurisdictionQuery {
	if _q == nil {
		return nil
	}
	return &JurisdictionQuery{
		config:                    _q.config,
		ctx:                       _q.ctx.Clone(),
		order:                     append([]jurisdiction.OrderOption{}, _q.order...),
		inters:                    append([]Interceptor{}, _q.inters...),
		predicates:                append([]predicate.Jurisdiction{}, _q.predicates...),
		withChildren:              _q.withChildren.Clone(),
		withParentJurisdiction:    _q.withParentJurisdiction.Clone(),
		withRules:                 _q.withRules.Clone(),
		withPropertyJurisdictions: _q.withPropertyJurisdictions.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithChildren tells the query-builder to eager-load the nodes that are connected to
// the "children" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *JurisdictionQuery) WithChildren(opts ...func(*JurisdictionQuery)) *JurisdictionQuery {
	query := (&JurisdictionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withChildren = query
	return _q
}

// WithParentJurisdiction tells the query-builder to eager-load the nodes that are connected to
// the "parent_jurisdiction" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *JurisdictionQuery) WithParentJurisdiction(opts ...func(*JurisdictionQuery)) *JurisdictionQuery {
	query := (&JurisdictionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withParentJurisdiction = query
	return _q
}

// WithRules tells the query-builder to eager-load the nodes that are connected to
// the "rules" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *JurisdictionQuery) WithRules(opts ...func(*JurisdictionRuleQuery)) *JurisdictionQuery {
	query := (&JurisdictionRuleClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRules = query
	return _q
}

// WithPropertyJurisdictions tells the query-builder to eager-load the nodes that are connected to
// the "property_jurisdictions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *JurisdictionQuery) WithPropertyJurisdictions(opts ...func(*PropertyJurisdictionQuery)) *JurisdictionQuery {
	query := (&PropertyJurisdictionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPropertyJurisdictions = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Jurisdiction.Query().
//		GroupBy(jurisdiction.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *JurisdictionQuery) GroupBy(field string, fields ...string) *JurisdictionGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &JurisdictionGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = jurisdiction.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Jurisdiction.Query().
//		Select(jurisdiction.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *JurisdictionQuery) Select(fields ...string) *JurisdictionSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &JurisdictionSelect{JurisdictionQuery: _q}
	sbuild.label = jurisdiction.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a JurisdictionSelect configured with the given aggregations.
func (_q *JurisdictionQuery) Aggregate(fns ...AggregateFunc) *JurisdictionSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *JurisdictionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !jurisdiction.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *JurisdictionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Jurisdiction, error) {
	var (
		nodes       = []*Jurisdiction{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withChildren != nil,
			_q.withParentJurisdiction != nil,
			_q.withRules != nil,
			_q.withPropertyJurisdictions != nil,
		}
	)
	if _q.withParentJurisdiction != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, jurisdiction.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Jurisdiction).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Jurisdiction{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withChildren; query != nil {
		if err := _q.loadChildren(ctx, query, nodes,
			func(n *Jurisdiction) { n.Edges.Children = []*Jurisdiction{} },
			func(n *Jurisdiction, e *Jurisdiction) { n.Edges.Children = append(n.Edges.Children, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withParentJurisdiction; query != nil {
		if err := _q.loadParentJurisdiction(ctx, query, nodes, nil,
			func(n *Jurisdiction, e *Jurisdiction) { n.Edges.ParentJurisdiction = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRules; query != nil {
		if err := _q.loadRules(ctx, query, nodes,
			func(n *Jurisdiction) { n.Edges.Rules = []*JurisdictionRule{} },
			func(n *Jurisdiction, e *JurisdictionRule) { n.Edges.Rules = append(n.Edges.Rules, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPropertyJurisdictions; query != nil {
		if err := _q.loadPropertyJurisdictions(ctx, query, nodes,
			func(n *Jurisdiction) { n.Edges.PropertyJurisdictions = []*PropertyJurisdiction{} },
			func(n *Jurisdiction, e *PropertyJurisdiction) {
				n.Edges.PropertyJurisdictions = append(n.Edges.PropertyJurisdictions, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *JurisdictionQuery) loadChildren(ctx context.Context, query *JurisdictionQuery, nodes []*Jurisdiction, init func(*Jurisdiction), assign func(*Jurisdiction, *Jurisdiction)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Jurisdiction)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Jurisdiction(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(jurisdiction.ChildrenColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.jurisdiction_children
		if fk == nil {
			return fmt.Errorf(`foreign-key "jurisdiction_children" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "jurisdiction_children" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *JurisdictionQuery) loadParentJurisdiction(ctx context.Context, query *JurisdictionQuery, nodes []*Jurisdiction, init func(*Jurisdiction), assign func(*Jurisdiction, *Jurisdiction)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Jurisdiction)
	for i := range nodes {
		if nodes[i].jurisdiction_children == nil {
			continue
		}
		fk := *nodes[i].jurisdiction_children
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(jurisdiction.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "jurisdiction_children" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *JurisdictionQuery) loadRules(ctx context.Context, query *JurisdictionRuleQuery, nodes []*Jurisdiction, init func(*Jurisdiction), assign func(*Jurisdiction, *JurisdictionRule)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Jurisdiction)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.JurisdictionRule(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(jurisdiction.RulesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.jurisdiction_rules
		if fk == nil {
			return fmt.Errorf(`foreign-key "jurisdiction_rules" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "jurisdiction_rules" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *JurisdictionQuery) loadPropertyJurisdictions(ctx context.Context, query *PropertyJurisdictionQuery, nodes []*Jurisdiction, init func(*Jurisdiction), assign func(*Jurisdiction, *PropertyJurisdiction)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Jurisdiction)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.PropertyJurisdiction(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(jurisdiction.PropertyJurisdictionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.jurisdiction_property_jurisdictions
		if fk == nil {
			return fmt.Errorf(`foreign-key "jurisdiction_property_jurisdictions" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "jurisdiction_property_jurisdictions" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *JurisdictionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *JurisdictionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(jurisdiction.Table, jurisdiction.Columns, sqlgraph.NewFieldSpec(jurisdiction.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, jurisdiction.FieldID)
		for i := range fields {
			if fields[i] != jurisdiction.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *JurisdictionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(jurisdiction.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = jurisdiction.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// JurisdictionGroupBy is the group-by builder for Jurisdiction entities.
type JurisdictionGroupBy struct {
	selector
	build *JurisdictionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *JurisdictionGroupBy) Aggregate(fns ...AggregateFunc) *JurisdictionGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *JurisdictionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JurisdictionQuery, *JurisdictionGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *JurisdictionGroupBy) sqlScan(ctx context.Context, root *JurisdictionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// JurisdictionSelect is the builder for selecting fields of Jurisdiction entities.
type JurisdictionSelect struct {
	*JurisdictionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *JurisdictionSelect) Aggregate(fns ...AggregateFunc) *JurisdictionSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *JurisdictionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JurisdictionQuery, *JurisdictionSelect](ctx, _s.JurisdictionQuery, _s, _s.inters, v)
}

func (_s *JurisdictionSelect) sqlScan(ctx context.Context, root *JurisdictionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
