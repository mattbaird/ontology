// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/matthewbaird/ontology/ent/jurisdiction"
	"github.com/matthewbaird/ontology/ent/jurisdictionrule"
	"github.com/matthewbaird/ontology/ent/predicate"
)

// JurisdictionRuleQuery is the builder for querying JurisdictionRule entities.
type JurisdictionRuleQuery struct {
	config
	ctx              *QueryContext
	order            []jurisdictionrule.OrderOption
	inters           []Interceptor
	predicates       []predicate.JurisdictionRule
	withJurisdiction *JurisdictionQuery
	withSupersededBy *JurisdictionRuleQuery
	withSupersedes   *JurisdictionRuleQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the JurisdictionRuleQuery builder.
func (_q *JurisdictionRuleQuery) Where(ps ...predicate.JurisdictionRule) *JurisdictionRuleQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *JurisdictionRuleQuery) Limit(limit int) *JurisdictionRuleQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *JurisdictionRuleQuery) Offset(offset int) *JurisdictionRuleQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *JurisdictionRuleQuery) Unique(unique bool) *JurisdictionRuleQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *JurisdictionRuleQuery) Order(o ...jurisdictionrule.OrderOption) *JurisdictionRuleQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryJurisdiction chains the current query on the "jurisdiction" edge.
func (_q *JurisdictionRuleQuery) QueryJurisdiction() *JurisdictionQuery {
	query := (&JurisdictionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(jurisdictionrule.Table, jurisdictionrule.FieldID, selector),
			sqlgraph.To(jurisdiction.Table, jurisdiction.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, jurisdictionrule.JurisdictionTable, jurisdictionrule.JurisdictionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySupersededBy chains the current query on the "superseded_by" edge.
func (_q *JurisdictionRuleQuery) QuerySupersededBy() *JurisdictionRuleQuery {
	query := (&JurisdictionRuleClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(jurisdictionrule.Table, jurisdictionrule.FieldID, selector),
			sqlgraph.To(jurisdictionrule.Table, jurisdictionrule.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, jurisdictionrule.SupersededByTable, jurisdictionrule.SupersededByColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySupersedes chains the current query on the "supersedes" edge.
func (_q *JurisdictionRuleQuery) QuerySupersedes() *JurisdictionRuleQuery {
	query := (&JurisdictionRuleClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(jurisdictionrule.Table, jurisdictionrule.FieldID, selector),
			sqlgraph.To(jurisdictionrule.Table, jurisdictionrule.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, jurisdictionrule.SupersedesTable, jurisdictionrule.SupersedesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first JurisdictionRule entity from the query.
// Returns a *NotFoundError when no JurisdictionRule was found.
func (_q *JurisdictionRuleQuery) First(ctx context.Context) (*JurisdictionRule, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{jurisdictionrule.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *JurisdictionRuleQuery) FirstX(ctx context.Context) *JurisdictionRule {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first JurisdictionRule ID from the query.
// Returns a *NotFoundError when no JurisdictionRule ID was found.
func (_q *JurisdictionRuleQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{jurisdictionrule.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *JurisdictionRuleQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single JurisdictionRule entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one JurisdictionRule entity is found.
// Returns a *NotFoundError when no JurisdictionRule entities are found.
func (_q *JurisdictionRuleQuery) Only(ctx context.Context) (*JurisdictionRule, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{jurisdictionrule.Label}
	default:
		return nil, &NotSingularError{jurisdictionrule.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *JurisdictionRuleQuery) OnlyX(ctx context.Context) *JurisdictionRule {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only JurisdictionRule ID in the query.
// Returns a *NotSingularError when more than one JurisdictionRule ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *JurisdictionRuleQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{jurisdictionrule.Label}
	default:
		err = &NotSingularError{jurisdictionrule.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *JurisdictionRuleQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of JurisdictionRules.
func (_q *JurisdictionRuleQuery) All(ctx context.Context) ([]*JurisdictionRule, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*JurisdictionRule, *JurisdictionRuleQuery]()
	return withInterceptors[[]*JurisdictionRule](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *JurisdictionRuleQuery) AllX(ctx context.Context) []*JurisdictionRule {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of JurisdictionRule IDs.
func (_q *JurisdictionRuleQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(jurisdictionrule.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *JurisdictionRuleQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *JurisdictionRuleQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*JurisdictionRuleQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *JurisdictionRuleQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *JurisdictionRuleQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *JurisdictionRuleQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the JurisdictionRuleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *JurisdictionRuleQuery) Clone() *JurisdictionRuleQuery {
	if _q == nil {
		return nil
	}
	return &JurisdictionRuleQuery{
		config:           _q.config,
		ctx:              _q.ctx.Clone(),
		order:            append([]jurisdictionrule.OrderOption{}, _q.order...),
		inters:           append([]Interceptor{}, _q.inters...),
		predicates:       append([]predicate.JurisdictionRule{}, _q.predicates...),
		withJurisdiction: _q.withJurisdiction.Clone(),
		withSupersededBy: _q.withSupersededBy.Clone(),
		withSupersedes:   _q.withSupersedes.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithJurisdiction tells the query-builder to eager-load the nodes that are connected to
// the "jurisdiction" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *JurisdictionRuleQuery) WithJurisdiction(opts ...func(*JurisdictionQuery)) *JurisdictionRuleQuery {
	query := (&JurisdictionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withJurisdiction = query
	return _q
}

// WithSupersededBy tells the query-builder to eager-load the nodes that are connected to
// the "superseded_by" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *JurisdictionRuleQuery) WithSupersededBy(opts ...func(*JurisdictionRuleQuery)) *JurisdictionRuleQuery {
	query := (&JurisdictionRuleClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSupersededBy = query
	return _q
}

// WithSupersedes tells the query-builder to eager-load the nodes that are connected to
// the "supersedes" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *JurisdictionRuleQuery) WithSupersedes(opts ...func(*JurisdictionRuleQuery)) *JurisdictionRuleQuery {
	query := (&JurisdictionRuleClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSupersedes = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.JurisdictionRule.Query().
//		GroupBy(jurisdictionrule.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *JurisdictionRuleQuery) GroupBy(field string, fields ...string) *JurisdictionRuleGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &JurisdictionRuleGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = jurisdictionrule.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.JurisdictionRule.Query().
//		Select(jurisdictionrule.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *JurisdictionRuleQuery) Select(fields ...string) *JurisdictionRuleSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &JurisdictionRuleSelect{JurisdictionRuleQuery: _q}
	sbuild.label = jurisdictionrule.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a JurisdictionRuleSelect configured with the given aggregations.
func (_q *JurisdictionRuleQuery) Aggregate(fns ...AggregateFunc) *JurisdictionRuleSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *JurisdictionRuleQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !jurisdictionrule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *JurisdictionRuleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*JurisdictionRule, error) {
	var (
		nodes       = []*JurisdictionRule{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withJurisdiction != nil,
			_q.withSupersededBy != nil,
			_q.withSupersedes != nil,
		}
	)
	if _q.withJurisdiction != nil || _q.withSupersededBy != nil || _q.withSupersedes != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, jurisdictionrule.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*JurisdictionRule).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &JurisdictionRule{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withJurisdiction; query != nil {
		if err := _q.loadJurisdiction(ctx, query, nodes, nil,
			func(n *JurisdictionRule, e *Jurisdiction) { n.Edges.Jurisdiction = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSupersededBy; query != nil {
		if err := _q.loadSupersededBy(ctx, query, nodes, nil,
			func(n *JurisdictionRule, e *JurisdictionRule) { n.Edges.SupersededBy = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSupersedes; query != nil {
		if err := _q.loadSupersedes(ctx, query, nodes, nil,
			func(n *JurisdictionRule, e *JurisdictionRule) { n.Edges.Supersedes = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *JurisdictionRuleQuery) loadJurisdiction(ctx context.Context, query *JurisdictionQuery, nodes []*JurisdictionRule, init func(*JurisdictionRule), assign func(*JurisdictionRule, *Jurisdiction)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*JurisdictionRule)
	for i := range nodes {
		if nodes[i].jurisdiction_rules == nil {
			continue
		}
		fk := *nodes[i].jurisdiction_rules
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(jurisdiction.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "jurisdiction_rules" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *JurisdictionRuleQuery) loadSupersededBy(ctx context.Context, query *JurisdictionRuleQuery, nodes []*JurisdictionRule, init func(*JurisdictionRule), assign func(*JurisdictionRule, *JurisdictionRule)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*JurisdictionRule)
	for i := range nodes {
		if nodes[i].jurisdiction_rule_superseded_by == nil {
			continue
		}
		fk := *nodes[i].jurisdiction_rule_superseded_by
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(jurisdictionrule.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "jurisdiction_rule_superseded_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *JurisdictionRuleQuery) loadSupersedes(ctx context.Context, query *JurisdictionRuleQuery, nodes []*JurisdictionRule, init func(*JurisdictionRule), assign func(*JurisdictionRule, *JurisdictionRule)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*JurisdictionRule)
	for i := range nodes {
		if nodes[i].jurisdiction_rule_superseded_by == nil {
			continue
		}
		fk := *nodes[i].jurisdiction_rule_superseded_by
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(jurisdictionrule.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "jurisdiction_rule_superseded_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *JurisdictionRuleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *JurisdictionRuleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(jurisdictionrule.Table, jurisdictionrule.Columns, sqlgraph.NewFieldSpec(jurisdictionrule.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, jurisdictionrule.FieldID)
		for i := range fields {
			if fields[i] != jurisdictionrule.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *JurisdictionRuleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(jurisdictionrule.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = jurisdictionrule.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// JurisdictionRuleGroupBy is the group-by builder for JurisdictionRule entities.
type JurisdictionRuleGroupBy struct {
	selector
	build *JurisdictionRuleQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *JurisdictionRuleGroupBy) Aggregate(fns ...AggregateFunc) *JurisdictionRuleGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *JurisdictionRuleGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JurisdictionRuleQuery, *JurisdictionRuleGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *JurisdictionRuleGroupBy) sqlScan(ctx context.Context, root *JurisdictionRuleQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// JurisdictionRuleSelect is the builder for selecting fields of JurisdictionRule entities.
type JurisdictionRuleSelect struct {
	*JurisdictionRuleQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *JurisdictionRuleSelect) Aggregate(fns ...AggregateFunc) *JurisdictionRuleSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *JurisdictionRuleSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JurisdictionRuleQuery, *JurisdictionRuleSelect](ctx, _s.JurisdictionRuleQuery, _s, _s.inters, v)
}

func (_s *JurisdictionRuleSelect) sqlScan(ctx context.Context, root *JurisdictionRuleQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
