// Code generated by cmd/entgen from CUE ontology. DO NOT EDIT.
package schema

import (
	"entgo.io/ent"
	"entgo.io/ent/schema/edge"
	"entgo.io/ent/schema/field"
	"entgo.io/ent/schema/mixin"
	"github.com/google/uuid"
	"github.com/matthewbaird/ontology/internal/types"
)

// Ensure mixin import is used.
var _ mixin.Schema

// LeaseSpace holds the schema definition for the LeaseSpace entity.
type LeaseSpace struct {
	ent.Schema
}

// Mixin of the LeaseSpace.
func (LeaseSpace) Mixin() []ent.Mixin {
	return []ent.Mixin{
		AuditMixin{},
	}
}

// Fields of the LeaseSpace.
func (LeaseSpace) Fields() []ent.Field {
	return []ent.Field{
		field.UUID("id", uuid.UUID{}).Default(uuid.New).Immutable().Comment("Primary key"),
		field.Bool("is_primary").Default(true),
		field.Enum("relationship").Values("primary", "expansion", "sublease", "shared_access", "parking", "storage", "loading_dock", "rooftop", "patio", "signage", "included", "membership"),
		field.JSON("effective", &types.DateRange{}),
		field.Float("square_footage_leased").Optional().Nillable(),
	}
}

// Edges of the LeaseSpace.
func (LeaseSpace) Edges() []ent.Edge {
	return []ent.Edge{
		edge.To("lease", Lease.Type).Unique().Required().Comment("LeaseSpace belongs to Lease"),
		edge.To("space", Space.Type).Unique().Required().Comment("LeaseSpace references Space"),
	}
}
